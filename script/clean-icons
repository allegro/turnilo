#!/usr/bin/env coffee

replace = require("replace")

replace({
  regex: "\\s+<!--[^>]+-->",
  replacement: '',
  paths: ['./icons/']
})

replace({
  regex: "\\s+<title>[^<]+</title>",
  replacement: '',
  paths: ['./icons/']
})
